import React from 'react';
import { MenuGrid } from '../../components'

const ComboMeals = () => {
  return(
    <div>
      <h1>ComboMeals</h1>
      <MenuGrid />
    </div>
  )
};

export default ComboMeals;
