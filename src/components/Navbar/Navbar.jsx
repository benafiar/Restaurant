import React from 'react'

const Navbar = () => {
  return(
    <h1>Navbar</h1>
  )
}
//make an api call to the sections to set state to map over the sections and populate this navbar with a component
export default Navbar