import React from 'react';
import { MenuGrid } from '../../components';

const PageView = ({ header, color }) => (
  <div>
    <MenuGrid header={header} color={color} />
  </div>
);

export default PageView;
